# Frontend Developer Skill Assessment

## 实施方案与验收计划

状态：P0 已实现，真实 Provider、浏览器生成闭环和生产启动入口已完成内部验收，当前等待最终用户验收。

## 1. 目标

在空白工作区中搭建一个 Angular 图片编辑器，完成以下核心闭环：

1. 集成 `Tui.ImageEditor`。
2. 用 Creaition 的黑白灰设计系统完成界面适配。
3. 通过 Angular 组件拆分工具栏、属性面板和 AI 面板。
4. 接入一个 AI 图片生成 API，默认选择 Hugging Face Stable Diffusion。
5. 用 RxJS 管理 Prompt、生成结果、加载状态、错误、历史记录和收藏。
6. 将生成图片加入编辑器画布。
7. 支持桌面端和移动端布局。
8. 准备在线 Demo、README 和演示截图。

目标不是在 4 小时内做出生产级图片编辑器，而是交付一个流程完整、视觉统一、能够被实际演示和检查的 coding test 版本。

## 2. 范围与优先级

### P0：4 小时内必须完成

- `Tui.ImageEditor` 基础编辑能力
- `ImageEditorComponent`
- `ToolbarComponent`
- `PropertiesPanelComponent`
- `AiGenerationPanelComponent`
- Creaition 颜色、字体、圆角、按钮和输入框规范
- Angular Material 组件的定制化使用
- 桌面端三栏布局
- 移动端工具栏折叠
- 移动端属性面板模态化
- 一个 AI 模型的真实请求路径
- Prompt 输入和生成结果展示
- 加载、成功、错误和重试状态
- RxJS 状态管理
- 将 AI 图片添加到编辑器画布
- 历史记录、收藏和 `localStorage`
- README、运行说明和验收截图

### P1：核心稳定后追加

- Prompt 建议和自动补全
- 更完整的模型参数面板
- 图生图入口
- 历史记录缩略图和重新编辑
- 下载生成图片
- 更清晰的 Token、网络、配额错误提示

### P2：只有在核心版本稳定后才做

- 多模型切换
- 批量生成
- 风格迁移
- AI 图片增强
- 更复杂的实时进度系统
- 多个 AI 平台的真实接入

时间不足时，P2 全部放弃，不影响核心版本交付。

## 3. 默认技术选择

### 前端

- Angular standalone components
- TypeScript
- SCSS
- Angular Material
- RxJS
- `Tui.ImageEditor` 3.x

### AI

默认使用 Hugging Face Stable Diffusion，因为题目将它列为推荐选项，且接入路径相对直接。

代码会把 AI 请求封装在独立的 `AiImageService` 中，组件不直接处理 HTTP 细节。这样后续可以替换成 Qwen 或 Google Imagen，而不需要重写界面。

### Token 配置

Token 不会放进聊天消息，也不会硬编码到组件中。开发时使用本地环境配置或占位配置；部署前按照 README 配置实际 Token。

由于纯前端部署会暴露请求凭据，README 中会注明生产环境应使用服务端代理。coding test 阶段优先保证真实请求、错误处理和可演示性。

如果 Token 暂时不可用，界面仍保留完整的 loading、error 和结果状态，便于先完成视觉和交互验收；拿到 Token 后再验证真实请求。

## 4. 视觉设计方案

### 设计关键词

专业、克制、编辑工具感、黑白高对比、内容优先。

### 设计 Token

| 用途 | 值 |
| --- | --- |
| 主背景 | `#f0f0f0` |
| 白色面板 | `#ffffff` |
| 主文字和强调 | `#000000` |
| 主灰色 | `#efefee` |
| 次灰色 | `#bebebe` |
| 字体 | `strokeWeight(var)`，备用 `Eina03-Regular` |
| 按钮圆角 | `50px` |
| 输入框圆角 | `0px` |
| 卡片圆角 | `1rem` |
| 最小按钮高度 | `35px` |
| 输入框高度 | `50px` |

### 页面结构

桌面端：

```text
┌────────────────────────────────────────────────────────────────┐
│ 品牌 / 文件名                         撤销  重做  导出          │
├──────────────┬───────────────────────────────┬─────────────────┤
│ 工具栏       │                               │ 属性 / AI 面板  │
│ 选择         │          图片编辑画布          │                 │
│ 裁剪         │                               │ Prompt          │
│ 文字         │                               │ 生成结果        │
│ 滤镜         │                               │ 历史 / 收藏     │
└──────────────┴───────────────────────────────┴─────────────────┘
```

移动端：

```text
┌──────────────────────────────┐
│ 品牌       文件名       导出  │
├──────────────────────────────┤
│                              │
│          图片编辑画布         │
│                              │
├──────────────────────────────┤
│       折叠后的底部工具栏       │
└──────────────────────────────┘
```

属性面板和 AI 面板在移动端以底部模态面板打开。

### 视觉原则

- 使用浅灰画布、白色工作面板和黑色控制元素形成层次。
- 只让 AI 生成区域成为视觉焦点，其余控件保持安静。
- 不制作营销式首页，不加入无关装饰或大面积渐变。
- 所有按钮和输入控件保持稳定尺寸，避免内容变化导致布局跳动。
- 悬停时改变字重和字体倾斜，体现题目要求的 `font-variation-settings`。
- 所有可交互元素保留键盘焦点状态。

## 5. 代码结构

计划使用以下边界：

```text
src/app/
├── core/
│   ├── ai-image.service.ts
│   ├── ai-state.service.ts
│   ├── storage.service.ts
│   └── models/
│       └── ai.models.ts
├── components/
│   ├── image-editor/
│   ├── toolbar/
│   ├── properties-panel/
│   └── ai-generation-panel/
├── app.component.ts
├── app.component.html
└── app.component.scss
```

### 关键状态

```text
prompt: string
selectedModel: string
modelParameters: object
generatedImages: ImageResult[]
history: ImageResult[]
favorites: string[]
isLoading: boolean
progressStage: 'idle' | 'preparing' | 'generating' | 'processing' | 'complete'
error: AiError | null
sourceImage: string | null
```

状态通过 RxJS Observable 暴露给组件，组件只触发动作，不直接修改共享状态。

### AI 请求流程

```text
用户输入 Prompt
        ↓
AiStateService 开始 loading
        ↓
AiImageService 使用 HttpClient 请求 API
        ↓
成功：解析 Base64 图片并写入历史
失败：分类错误并提供重试操作
        ↓
AiGenerationPanel 展示结果
        ↓
用户点击“加入画布”
        ↓
ImageEditorComponent 调用 Tui.ImageEditor 加载图片
```

指数退避只实现必要的 1 至 2 次重试，避免在有限时间内制作复杂任务队列。同步 AI API 没有真实流式进度时，界面显示明确的阶段状态和加载指示器，不伪造后端百分比。

## 6. 四小时执行计划

### 0:00 至 0:15：初始化和风险验证

- 创建 Angular 项目
- 安装 `Tui.ImageEditor`、Angular Material 和图标依赖
- 检查项目能否启动
- 验证 Hugging Face Token 和 Endpoint
- 建立基础环境配置

时间门槛：15 分钟后必须能启动空白 Angular 页面，否则立即处理环境问题，不继续堆代码。

### 0:15 至 1:00：编辑器基础骨架

- 初始化 `ImageEditorComponent`
- 绑定画布容器
- 加入上传图片和默认演示图
- 创建顶部导航、左侧工具栏和右侧面板
- 完成基本的编辑器尺寸和溢出处理

验收结果：可以打开页面、看到编辑画布和基本工具区。

### 1:00 至 1:45：Creaition 视觉适配

- 设置全局颜色和字体变量
- 统一按钮、输入框、卡片和面板样式
- 完成 Toolbar 样式
- 完成 PropertiesPanel 样式
- 添加悬停字重和倾斜效果
- 调整 Tui.ImageEditor 的内部主题

验收结果：桌面端第一眼能看出黑白灰品牌系统，而不是默认 Angular 或默认 TUI 样式。

### 1:45 至 2:30：AI 服务和状态管理

- 编写 `AiImageService`
- 编写 `AiStateService`
- 添加 Prompt、loading、success、error 状态
- 添加 HTTP 错误映射
- 添加必要的指数退避重试
- 添加历史记录、收藏和 `localStorage`

验收结果：AI 面板状态流转完整，即使 API 失败也不会卡死页面。

### 2:30 至 3:15：AI 面板和画布集成

- 完成 Prompt 输入
- 添加推荐 Prompt
- 添加生成按钮和 loading 状态
- 展示 Base64 图片结果
- 添加“加入画布”操作
- 添加收藏和历史操作
- 处理空状态和错误状态

验收结果：从输入 Prompt 到图片进入画布的主流程跑通。

### 3:15 至 3:40：响应式和移动端

- 按 `640px`、`768px`、`1024px`、`1280px` 调整布局
- 移动端折叠工具栏
- 移动端属性面板改成模态面板
- 检查按钮、输入框和文字是否溢出
- 检查画布是否适配屏幕

验收结果：桌面端和移动端都能完成一次生成流程。

### 3:40 至 4:00：冻结功能并准备验收

- 修复控制台错误
- 检查刷新后偏好设置是否保留
- 检查 API 失败提示和重试按钮
- 完成 README
- 准备截图和演示顺序
- 如果环境已准备好，启动部署

4 小时到点后冻结 P0，不再为了增加功能破坏已有流程。

## 7. 额外一至两小时的 P2 安排

只有以下检查全部通过后才进入 P2：

- AI 真实生成成功
- 图片能加入画布
- 移动端布局正常
- 刷新后历史记录和收藏仍然存在
- README 和 Demo 可访问
- 没有阻塞性的控制台错误

追加顺序：

1. 多模型切换界面，但先复用统一请求接口。
2. 批量生成 2 至 4 张图片。
3. 图生图入口。
4. 风格迁移或 AI 增强的展示入口。

如果每项不能在 20 至 30 分钟内完成，就停止该项并回到验收修复。

## 8. 验收演示流程

建议控制在 2 至 3 分钟：

1. 打开桌面端，展示品牌配色、字体、工具栏和编辑画布。
2. 上传或打开默认图片，展示基础编辑能力。
3. 打开 AI 面板，选择推荐 Prompt。
4. 点击生成，展示 loading 和阶段状态。
5. 生成成功后展示结果缩略图。
6. 将生成图片加入 Tui 画布。
7. 收藏结果，刷新页面，展示历史和偏好仍然存在。
8. 触发一次错误或展示错误处理设计。
9. 切换到移动端尺寸，展示工具栏折叠和属性模态面板。
10. 最后展示 GitHub、在线 Demo、README 和截图。

## 9. 你需要配合的内容

现在不需要你准备完整需求文档，也不需要把 API Token 发到聊天里。

在 AI 接入阶段，你需要：

- 确认是否有 Hugging Face Token
- 按我提供的文件路径在本机配置 Token
- 启动项目并把浏览器错误或截图发给我
- 对视觉验收给出“通过”或指出具体问题

在部署阶段，你需要：

- 登录 GitHub
- 登录 Vercel 或 Netlify
- 按部署页面完成授权
- 把部署后的 URL 发回给我进行最终检查

可选配合：如果你有希望用于演示的图片，可以提供一张本地图片；没有也不影响开发，我会准备默认演示内容。

## 10. 时间管理规则

- 15 分钟无法启动项目：先解决环境，不增加业务代码。
- 2 小时还没有稳定编辑器界面：立即砍掉所有 P1 和 P2。
- 3 小时 AI 主流程没有跑通：停止视觉微调，优先完成 API、状态和错误闭环。
- 3 小时 40 分钟开始只做修复、README、截图和部署。
- 不接入三个 AI 平台。
- 不从零实现图片编辑器功能，尽量复用 Tui API。
- 不为了追求高级功能牺牲核心演示流程。

最终验收标准是：界面像一个完成度高的图片工具，AI 生成流程真实可用，失败时有反馈，移动端可操作，提交材料齐全。

## 11. 可执行 SOP 与阶段状态

### 11.1 状态定义

每个阶段只能使用以下状态：

- `未开始`：尚未执行。
- `进行中`：正在执行，尚未完成全部自测。
- `待修复`：自测发现问题，不能进入下一阶段。
- `待用户验收`：内部自测全部通过，需要用户确认视觉或外部环境结果。
- `已完成`：内部自测和必要的用户验收都通过。
- `已跳过`：只允许用于 P1/P2，不允许用于 P0。

阶段状态记录格式：

```text
阶段：
状态：未开始 / 进行中 / 待修复 / 待用户验收 / 已完成 / 已跳过
完成时间：
阻塞问题：无 / 问题描述
验证证据：命令结果、截图、URL 或复现步骤
```

### 11.2 阶段 0：项目启动与依赖门禁

状态：`已完成`

目标：确保项目可以安装、启动和编译，避免后续在错误的工程基础上堆功能。

执行内容：

- 创建 Angular 项目和基础目录。
- 安装 Angular Material、`Tui.ImageEditor`、图标依赖。
- 建立环境变量示例和 README 骨架。
- 固定可用的 Node、Angular 和依赖版本。

交付物：

- 可安装的 `package.json` 和 lockfile。
- 可启动的 Angular 空白页面。
- `.env.example` 或等效的本地配置说明。
- README 初稿。

自测点：

- [ ] `npm install` 或 `npm ci` 成功结束。
- [ ] 开发服务器能够启动。
- [ ] 浏览器可以访问本地页面。
- [ ] 首次构建没有 TypeScript 或模板编译错误。
- [ ] 控制台没有阻塞性错误。
- [ ] Angular Material 可以被正常导入。
- [ ] `Tui.ImageEditor` 可以被打包，不出现模块解析错误。
- [ ] 没有把真实 Token 写入 Git 跟踪文件。

通过门槛：项目可启动、可构建、依赖可解析。

失败处理：优先锁定兼容版本、清理错误依赖、检查 Node 版本和构建配置。若单个图标或非核心依赖阻塞超过 10 分钟，先移除该依赖，保留核心功能路径。

### 11.3 阶段 1：Tui 图片编辑器和页面骨架

状态：`已完成`

目标：用户可以打开页面、看到编辑画布，并完成至少一次图片载入和基础编辑。

执行内容：

- 创建 `ImageEditorComponent`。
- 初始化 TUI 编辑器实例，并在组件销毁时释放资源。
- 加入默认演示图片和本地图片上传。
- 创建顶部导航、工具栏区域和右侧面板区域。
- 处理画布尺寸、缩放和容器溢出。

交付物：

- 可以显示 Tui 编辑器的页面。
- 可上传或加载图片的入口。
- 基础工具栏和属性面板占位结构。
- 清晰的组件边界。

自测点：

- [ ] 页面首次加载时编辑器可见。
- [ ] 默认演示图片可以加载。
- [ ] 上传合法图片后画布更新。
- [ ] 上传非图片文件时有明确提示。
- [ ] 画布不会撑破页面或产生不可控横向滚动。
- [ ] 组件销毁后没有明显订阅或编辑器实例泄漏。
- [ ] 至少验证选择、裁剪或滤镜中的两项基础编辑操作。
- [ ] 浏览器刷新后页面仍可重新初始化。

通过门槛：上传图片、编辑图片和刷新重载三条路径均可运行。

失败处理：先隔离 TUI 初始化问题，再处理样式。不要从零重写编辑器功能；优先查 TUI 的实际 API 和生命周期。

### 11.4 阶段 2：Creaition 视觉设计适配

状态：`已完成`

目标：让评审第一眼看到一个统一的 Creaition 风格工作台，而不是默认 Angular 或默认 TUI 页面。

执行内容：

- 设置颜色、字体、尺寸和圆角设计变量。
- 重写工具栏、面板、输入框、按钮和状态区域样式。
- 对 TUI 内部控件做必要的主题覆盖。
- 添加悬停字重、字体倾斜和键盘焦点效果。
- 保持信息密度和对齐关系稳定。

交付物：

- Creaition 黑白灰主题。
- 完成样式的 Toolbar、PropertiesPanel 和页面外壳。
- 设计变量集中定义，避免组件中散落颜色值。
- 桌面端基准截图。

自测点：

- [ ] 主背景为 `#f0f0f0`。
- [ ] 主文字和强调色为 `#000000`。
- [ ] 主灰色、次灰色和白色均有实际使用。
- [ ] 字体声明包含 `strokeWeight(var)` 和备用字体。
- [ ] 至少一个交互控件使用 `font-variation-settings`。
- [ ] 按钮圆角为 `50px`。
- [ ] 输入框圆角为 `0px`。
- [ ] 按钮高度不低于 `35px`。
- [ ] 输入框高度为 `50px`。
- [ ] 悬停、聚焦和禁用状态可辨识。
- [ ] 长文本、错误提示和按钮文字不会溢出。
- [ ] 没有无关的营销区块、渐变背景或装饰性元素。
- [ ] 1440px 和 1024px 宽度下布局没有重叠。

通过门槛：主要页面、工具栏和 AI 面板使用同一套视觉规则；没有明显的默认组件残留或重叠。

失败处理：先修复布局和对比度，再做细节微调。任何会影响可读性、点击或内容溢出的视觉问题都属于 P0 问题。

### 11.5 阶段 3：AI 服务和 RxJS 状态管理

状态：`已完成`

目标：AI 生成请求可以被可靠地发起、取消或结束，并且成功、加载和失败状态不会互相污染。

执行内容：

- 创建 `AiImageService`，统一封装 HttpClient 请求。
- 创建 `AiStateService`，集中维护共享状态。
- 添加 Prompt、模型、参数、图片、历史、收藏和错误状态。
- 实现有限次数的指数退避重试。
- 对成功响应、API 错误、配额错误、网络错误和配置错误进行分类。
- 处理 localStorage 读写和异常数据。

交付物：

- 可复用的 AI Service。
- 可观察的 RxJS 状态流。
- AI 模型和参数类型定义。
- Token 配置说明。
- 可展示的 loading、error 和 retry 状态。

自测点：

- [ ] 有效 Token 和有效 Prompt 能发起请求。
- [ ] 请求开始后按钮进入 loading 状态并防止重复提交。
- [ ] 请求成功后 loading 一定结束。
- [ ] 请求失败后 loading 一定结束。
- [ ] 无 Token 时显示可理解的配置提示。
- [ ] 非法 Prompt 不会发起空请求。
- [ ] 网络错误会显示可理解的错误信息。
- [ ] 401/403 错误提示认证问题。
- [ ] 429 错误提示配额或速率限制问题。
- [ ] 5xx 错误可以按规则重试，且不会无限重试。
- [ ] 重试过程中状态不会重复写入历史。
- [ ] 组件重复订阅不会造成重复请求或重复结果。
- [ ] localStorage 缺失、损坏或超限时页面仍能使用。
- [ ] 状态刷新后可以恢复历史、收藏和用户偏好。
- [ ] Token 不出现在日志、截图、README 或 Git 记录中。

通过门槛：至少完成一次真实成功请求和一次真实失败请求的验证，且所有请求路径最终都会回到可操作状态。

失败处理：先通过网络响应、状态码和响应体定位原因。若外部 API 本身不可用，保留真实请求路径并启用仅用于视觉验收的演示结果，同时在 README 标明配置状态，不把演示模式伪装成真实生成。

### 11.6 阶段 4：AI 面板和画布闭环

状态：`已完成`

目标：完成“输入 Prompt、生成图片、查看结果、收藏、加入画布”的完整用户流程。

执行内容：

- 创建 `AiGenerationPanelComponent`。
- 添加 Prompt 输入、推荐 Prompt、生成按钮和状态反馈。
- 显示生成结果缩略图。
- 实现收藏、历史和加入画布。
- 支持从编辑器当前图片进入图生图入口，若 API 能力和时间允许。

交付物：

- 可用的 AI 生成面板。
- 生成结果列表。
- 历史记录和收藏交互。
- 生成图片加入 TUI 画布的操作。
- 空状态、加载状态、成功状态和错误状态。

自测点：

- [ ] 空 Prompt 点击生成时不会发请求，并显示提示。
- [ ] 推荐 Prompt 可以一键填入输入框。
- [ ] 生成期间按钮、进度文本和结果区状态一致。
- [ ] 成功结果可以预览。
- [ ] 失败结果不会覆盖上一次可用结果。
- [ ] 点击收藏后状态立即变化。
- [ ] 取消收藏后刷新仍保持正确状态。
- [ ] 历史记录按生成时间排序且不会重复插入。
- [ ] 点击加入画布后图片确实出现在 TUI 画布中。
- [ ] 加入画布失败时有明确反馈，面板仍可继续生成。
- [ ] 刷新页面后历史和收藏仍可见。
- [ ] 连续快速点击不会产生重复任务。
- [ ] 生成后可以继续编辑或生成下一张图片。

通过门槛：完整主流程至少连续成功两次，失败后重试一次也能恢复。

失败处理：优先修复状态机和数据流，不用新增功能掩盖主流程问题。必要时减少结果操作数量，但必须保留加入画布、收藏和历史中的核心路径。

### 11.7 阶段 5：响应式和移动端验收

状态：`已完成`

目标：核心功能在桌面端和移动端均可操作，不出现遮挡、溢出或无法关闭的面板。

执行内容：

- 实现 `640px`、`768px`、`1024px` 和 `1280px` 断点。
- 移动端折叠工具栏。
- 移动端使用模态面板展示属性和 AI 设置。
- 处理画布宽高、自适应和触控尺寸。

交付物：

- 桌面端截图。
- 移动端截图。
- 响应式样式和模态面板。
- 移动端演示路径。

自测点：

- [ ] 1440px 下三栏布局稳定。
- [ ] 1024px 下侧栏和画布不重叠。
- [ ] 768px 下布局切换符合预期。
- [ ] 640px 以下工具栏折叠或转为底部操作区。
- [ ] 390px 宽度下没有横向滚动。
- [ ] 移动端属性面板可以打开。
- [ ] 移动端属性面板可以关闭。
- [ ] 打开模态面板时背景滚动不会干扰操作。
- [ ] Prompt 输入框、按钮和错误文本不溢出。
- [ ] 移动端仍可完成生成和加入画布。
- [ ] 键盘焦点或触控反馈清晰可见。

通过门槛：至少在 1440px 和 390px 两个基准尺寸完整走通一次核心演示。

失败处理：优先保证可操作性和内容不遮挡，其次再优化间距。无法在移动端保留所有桌面控件时，保留主要编辑、生成、历史和加入画布操作。

### 11.8 阶段 6：P0 冻结、交付材料和最终回归

状态：`已完成`

目标：冻结核心功能，确保项目可安装、可构建、可演示、可提交。

执行内容：

- 执行完整构建和生产构建。
- 检查控制台、网络请求和关键交互。
- 完善 README。
- 生成桌面端和移动端截图。
- 完成在线部署，若部署账号已经准备好。
- 按最终演示顺序重新走一遍。

交付物：

- 可运行的源代码。
- README。
- 在线 Demo URL，或明确的本地运行说明。
- AI 生成内容截图。
- 最终验收记录。

自测点：

- [ ] 全新目录可以按 README 启动项目。
- [ ] 生产构建成功。
- [ ] 页面首次加载无阻塞性异常。
- [ ] 默认图片和上传图片都能工作。
- [ ] AI 成功路径能工作。
- [ ] AI 失败路径能恢复。
- [ ] 加入画布能工作。
- [ ] 历史、收藏和刷新恢复能工作。
- [ ] 桌面端和移动端均无明显重叠或溢出。
- [ ] README 写明 API 配置、功能范围和已知限制。
- [ ] 截图中没有 Token、调试信息或未完成占位内容。
- [ ] Git 状态中没有 `.env`、Token 或临时文件。
- [ ] 演示流程可以在 3 分钟内完成。

通过门槛：P0 自测项全部通过，才允许开始 P2；若部署受账号或网络阻塞，必须保留可复现的本地运行结果。

失败处理：冻结新增功能，按照“构建问题、核心流程问题、响应式问题、交付材料问题”的顺序修复。修复后至少重新执行受影响阶段和最终主流程。

### 11.9 阶段 7：P2 加分功能

状态：`已跳过`

进入条件：阶段 6 已完成。

执行顺序：

1. 多模型切换。
2. 批量生成 2 至 4 张图片。
3. 图生图。
4. 风格迁移或 AI 增强。

每个 P2 功能必须单独满足以下自测点：

- [ ] 新功能有清晰入口。
- [ ] 新功能不会破坏 P0 主流程。
- [ ] loading、成功和错误状态完整。
- [ ] 刷新后数据状态仍然合理。
- [ ] 移动端入口不会遮挡主要操作。
- [ ] 没有新增控制台错误。
- [ ] README 已说明该功能。

任一项出现回归，就立即撤回该 P2 功能或恢复到上一个稳定版本。

## 12. 需求到验收的追踪关系

| 题目要求 | 对应阶段 | 必须提供的证据 |
| --- | --- | --- |
| Tui 图片编辑器集成 | 阶段 1、4 | 编辑器截图、上传和加入画布演示 |
| Creaition 设计系统 | 阶段 2 | 桌面端截图、设计变量和交互状态 |
| Angular 组件拆分 | 阶段 1、2、4 | 组件目录和运行页面 |
| 响应式设计 | 阶段 5 | 1440px 和 390px 截图及演示 |
| AI API 调用 | 阶段 3、4 | 成功请求、结果图片和错误处理 |
| RxJS 状态管理 | 阶段 3、4 | 状态流转和重复请求测试 |
| 历史、收藏、本地存储 | 阶段 3、4、6 | 刷新恢复演示 |
| README、Demo、截图 | 阶段 6 | 仓库、URL、README 和截图 |
| 高级 AI 功能 | 阶段 7 | 单独功能演示，可因时间跳过 |

## 13. 问题处理与确认规则

遇到问题时按以下顺序处理：

1. 先复现问题并记录实际错误、触发步骤和影响范围。
2. 判断是环境、依赖、代码、API、数据状态还是视觉布局问题。
3. 优先由 Codex 自主修复并重新执行受影响阶段的自测。
4. 修复后做回归测试，确认没有破坏已经通过的 P0 流程。
5. 如果同一问题连续两次修复仍失败，再缩小范围或降低非核心功能复杂度。
6. 只有遇到外部凭据、账号授权、产品方向或原文含义不明确时才向用户确认。

必须向用户确认的情况：

- 没有任何可用 AI Token，无法完成真实 API 验证。
- GitHub、Vercel 或 Netlify 需要用户本人授权。
- 题目要求和实际 API 能力发生冲突，且会影响技术路线。
- 需要在两个同等重要的视觉方向之间做选择。

不需要向用户确认的情况：

- 普通 TypeScript、Angular、CSS 或依赖错误。
- 组件拆分、文件命名和内部实现细节。
- 一般的响应式布局调整。
- 常规错误处理、loading 状态和数据校验。

## 14. 最终完成定义

整个任务只有同时满足以下条件，才标记为 `已完成`：

- 阶段 0 至阶段 6 全部完成。
- P0 自测点没有未解释的失败项。
- AI 主流程至少成功运行一次，并验证过失败恢复。
- 图片确实能够进入 Tui 编辑器画布。
- 桌面端和移动端都能完成核心流程。
- 刷新后历史、收藏和偏好设置保持合理。
- README、截图和运行方式齐全。
- 没有把 Token、调试信息或临时文件提交到仓库。
- 最终演示可以在 3 分钟内连贯完成。

如果 P2 没有完成，最终状态仍然可以是 `已完成`，但 README 和验收记录中必须明确说明 P2 未实现或仅实现了部分功能。

## 15. 本次执行状态

更新时间：2026-09-04

| 阶段 | 状态 | 验证结果 |
| --- | --- | --- |
| 阶段 0：项目启动与依赖门禁 | `已完成` | 依赖安装成功，`npm run build` 成功 |
| 阶段 1：Tui 图片编辑器和页面骨架 | `已完成` | 画布、默认图、上传和基础编辑通过 |
| 阶段 2：Creaition 视觉设计适配 | `已完成` | 桌面端和移动端截图已生成，视觉检查通过 |
| 阶段 3：AI 服务和 RxJS 状态管理 | `待用户验收` | Demo、模拟 Blob 成功响应、无效 Token 错误已通过；真实 Token 尚未验证 |
| 阶段 4：AI 面板和画布闭环 | `已完成` | 生成、预览、收藏、历史和加入画布通过 |
| 阶段 5：响应式和移动端验收 | `已完成` | 390px 面板开关和生成流程通过 |
| 阶段 6：P0 冻结、交付材料和最终回归 | `进行中` | README 和截图已完成，部署等待外部账号授权 |
| 阶段 7：P2 加分功能 | `已跳过` | 按时间规则保留为可选项 |

### 已执行的自动化验证

- 桌面端 TUI 容器真实渲染。
- 默认图片加载和本地图片上传。
- 添加文字、裁剪、旋转、灰度、撤销和重做。
- Demo mode 生成和加入画布。
- 模拟真实图片 Blob 响应解析。
- 无效 Token 的认证错误提示。
- 收藏、历史和刷新恢复。
- 移动端面板打开、关闭和生成。
- 浏览器控制台错误数为 0。
- 生产构建成功。

### 尚需外部条件的验证

- 配置有效 Hugging Face Token 后执行一次真实生成。
- 登录 GitHub 与 Vercel 或 Netlify 后完成在线部署。
